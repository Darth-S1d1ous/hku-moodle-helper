{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "login",
    "description": "Login window loads HKU Moodle/CAS. Empty permissions so that origin cannot call app commands.",
    "windows": ["login"],
    "permissions": []
}